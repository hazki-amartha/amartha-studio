# dependencies
/node_modules

# next.js
/.next/
# Verification builds (NEXT_DIST_DIR), kept out of the dev server's cache.
/.next-verify/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*

# env
.env
.env*.local

# typescript
*.tsbuildinfo
next-env.d.ts

# scratch output
/scripts/.check-flows-bundle.mjs
/scripts/.check-flows-bundle.css

# Figma-exported component specs — /ingest reads them from here, but they are
# working input, not source. Tracking them put 14MB of PDFs in every clone.
# Keep them locally; they don't travel with the repo.
/design-system/raw/

# agent worktrees
/.claude/worktrees/
.vercel
.env*
.gstack/
