import type { ProjectConfig } from '@/platform/types'

export const config: ProjectConfig = {
  slug: 'ngmis-live',
  name: 'NG-MIS — Live Reference',
  platform: 'NGMIS',
  owner: 'Hazki',
  description:
    'Source of truth for the NG-MIS back-office shell — header, sidebar, tabs and data table, rebuilt in FunDS Lite at 1440×900 so a desktop proposal is judged against the real console rather than a memory of it.',
  device: 'desktop',
  status: 'live',
  createdAt: '2026-08-04',
}
