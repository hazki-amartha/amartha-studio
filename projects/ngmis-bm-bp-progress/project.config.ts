import type { ProjectConfig } from '@/platform/types'

export const config: ProjectConfig = {
  slug: 'ngmis-bm-bp-progress',
  name: 'NGMIS BM — BP Progress Board',
  platform: 'NGMIS',
  owner: 'Hazki',
  description:
    "The Branch Manager's daily read of her own BPs, as one board instead of the national BP Loan sheet she filters down today. Ten rows, three column groups — tasks today, repayment this week against the real per-DPD targets, disbursement this month — and a drill-down that ends on the reason a mitra did not pay, as the BP recorded it in A-Partner.",
  device: 'desktop',
  status: 'draft',
  createdAt: '2026-08-06',
}
