import type { ProjectConfig } from '@/platform/types'

export const config: ProjectConfig = {
  slug: 'sample-topup',
  name: 'Sample — Poket Top Up',
  owner: 'Hazki',
  description:
    'WS-0 stub project used by the platform workstreams as a real render target. Replaced by the WS-G acid-test project before launch.',
  device: 'mobile',
  status: 'draft',
  createdAt: '2026-07-15',
  notes: ['Stub project for platform development — not a real design exploration.'],
}
