# NOTES

Project-local components (CLAUDE.md §4), copied (not imported — §1) and trimmed
from `projects/ngmis-bm-monitoring/lib/shell.tsx` / `lib/ui.tsx`:

- `BmShell` / `SideNav` / `Breadcrumbs` / `PageHeading` (`lib/shell.tsx`) — the desktop chrome, lands on Branches ▸ POI creation.
- `Panel` / `PanelHeading` / `Select` / `SimpleTable` / `EmptyState` (`lib/ui.tsx`) — FunDS has no table or select; same promotion candidates as `ngmis-bm-monitoring`.
